# The docs site is intended to be served under a subpath of the marketing site
# rather than at the root of its own origin. Set BASE_PATH to that subpath when
# building so asset URLs, links, and the client-side search index resolve.
#
# Leave it unset for local development (`pnpm dev` serves at http://localhost:3000/).
# BASE_PATH=/docs
