[package]
name = "tidebreak-egress"
description = "Tidebreak egress policy: one dependency-free decision layer for every enforcement point."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lib]
# No executable documentation examples; behavior contracts live in the test
# suite, so skip the doctest compile step.
doctest = false
[lints]
workspace = true

[dependencies]
