harness = "grok"
version = "1.0.13"
date = "2026-09-08"
observed_version_command = "grok --version"
observed_version = "grok 1.0.13 (5e9a58528b76) [stable]"
integration_path = "print-streaming-json-and-acp-stdio"
scenarios = ["image-read", "acp-approve", "acp-deny", "acp-cancel", "acp-resume", "acp-plan"]

[capture]
notes = """
The installed official CLI runs in an isolated GROK_HOME against a localhost
Chat Completions fixture. The fixture requests read_file for a generated
64-by-64 PNG, then records the next outbound model request. No external model
runs. The tool update contains image content, and the next request contains
the same PNG bytes in an image_url block. The probe qualifies tool image
transport; it does not qualify model vision quality or direct user attachments.

The ACP captures use grok agent --no-leader stdio with a separate localhost
fixture, fresh HOME, and non-loopback network access denied. The provider
requests one harmless printf command. allow_once creates its file;
reject_once and session/cancel leave the file absent. A separate capture
also verifies session/load after restarting the child, resetting a previously
always-approve native session to supervised permissions, and confirming
session/set_model and session/set_mode. Usage fields come from scripted
provider accounting. No external model runs.
"""

[redaction]
notes = """
Local paths become /workspace/capture.png. Session and request identifiers
become fixture identifiers. available_commands entries are removed because
they name unrelated local skills. The recorded request retains only its model
and image-bearing tool message. It omits system text, tools, and headers.
The image is a generated solid red square. Its bytes are unchanged.
ACP records retain both directions, exact one-use option IDs, tool inputs,
and terminal outcomes. Machine paths and session identifiers are replaced.
Unrelated model catalogs, command inventories, and timestamp metadata are omitted.
"""

verified = [
  "ACP initialize/session/new and session/load accept the captured shapes",
  "ACP allow_once executes the exact command; reject_once leaves it unexecuted",
  "ACP session/cancel interrupts a waiting tool without executing it",
  "read_file accepts target_file and reads a local PNG in headless mode",
  "tool_call_update carries image content and ReadFile.ImageContent",
  "the next Chat Completions request carries a tool-role image_url block",
  "the image data URL carries the same PNG bytes as the tool update",
]

not_verified = [
  "real model visual interpretation",
  "direct user image attachments through the Tidebreak adapter",
  "earlier Grok versions",
]

[sign_in_help]
file = "sign-in-help.txt"
command = "grok login --help"
captured = "2026-09-23"
binary = "The pinned @xai-official/grok@1.0.13 install under the Tidebreak data directory"
result = "`grok login` signs in to Grok; --oauth and --device-auth choose the flow."

[acp_plan]
file = "acp-plan.ndjson"
captured = "2026-09-23"
binary = "The pinned wrapper run with a fresh GROK_HOME, so it unpacked and ran its own 1.0.13 payload; `grok --version` printed `grok 1.0.13 (5e9a58528b76)`"
command = "grok agent --no-leader --model grok-4.6 --reasoning-effort high stdio"
provider = "Scripted localhost Responses provider reached through GROK_XAI_API_BASE_URL, with XAI_API_KEY set to a placeholder. Non-loopback network was denied. No live model ran."
notes = """
The scripted model calls todo_write twice: three todos, then a merge that
marks them completed, in_progress, and cancelled. Each call arrives as its own
tool_call and tool_call_update frames, and a `plan` session update restates
the whole list after each. A third `plan` update, sent just before the prompt
answer, marks every entry completed. Plan entries carry no ids, priority is
always medium, and a cancelled todo becomes status completed with
_meta.cancelled. The completed todo_write carries the list as
rawOutput.TodosUpdated.summary_for_prompt.

The initialize answer is kept whole. Its _meta.modelState.availableModels
lists each model with _meta.reasoningEfforts: grok-4.6 takes low, medium,
high, and xhigh; grok-4.5 takes low, medium, and high. With the relay's
GROK_MODELS_BASE_URL instead, the list comes only from that endpoint's
/models rows, which state no ladder.
"""
redactions = """
The workspace path became /workspace; the session id fixture-session,
including eventId values; the prompt id fixture-request-1; the host name
fixture-host; agentId fixture-agent; agentInstanceId fixture-agent-instance.
No key or token appeared. Frame order and every other value are as emitted.
"""

[binary_resolution]
checked = "2026-09-23"
binary = "The pinned @xai-official/grok@1.0.13 install under the Tidebreak data directory, run in place and never changed"
notes = """
The npm entrypoint (node_modules/.bin/grok) runs $GROK_HOME/bin/grok, with
GROK_HOME defaulting to ~/.grok, and unpacks its bundled binary there as
bin/grok-<version> only when that file is missing. Grok's own updater keeps
~/.grok/bin/grok current, and the npm install script also writes it.

On the checking machine, ~/.grok/bin/grok pointed at grok-1.0.40:
- the entrypoint with the person's home printed `grok 1.0.40 (eb1a2256660d)
  [stable]`;
- the entrypoint with GROK_HOME set to a fresh directory unpacked
  bin/grok-1.0.13 there in about a second and printed `grok 1.0.13
  (5e9a58528b76)`;
- that unpacked binary, run with the person's own home, printed `grok 1.0.13
  (5e9a58528b76) [stable]`, and its `grok models` reported `You are logged in
  with grok.com.` with the default model from the person's config.toml;
- the same binary with the fresh GROK_HOME reported `You are not
  authenticated.`, because Grok keeps sign-in in $GROK_HOME/auth.json.

GROK_DISABLE_AUTOUPDATER=1 turns off the update check for one process. The
binary's own documentation says a stdio agent skips its background update
unless it runs from $GROK_HOME/bin/grok.
"""
