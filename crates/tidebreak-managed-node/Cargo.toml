[package]
name = "tidebreak-managed-node"
description = "Tidebreak managed Node runtime: the shared verification contract for the pinned Node install."
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
