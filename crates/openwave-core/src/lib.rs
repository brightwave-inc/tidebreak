//! OpenWave core — the agent loop, tool registry, provider adapters, and the
//! `Store` / `BlobStore` / `SecretProvider` / `Auth` traits every client sits on.
//!
//! Scaffolding only. Implementation lands with the M0 walking skeleton.
