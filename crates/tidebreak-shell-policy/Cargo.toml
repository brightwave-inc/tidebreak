[package]
name = "tidebreak-shell-policy"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "Deterministic allow/ask/deny analysis for shell commands against standing rules."
publish = false

[lib]
# No executable documentation examples; behavior contracts live in the test
# suite, so skip the doctest compile step.
doctest = false
[lints]
workspace = true

[dependencies]
brush-parser = { workspace = true }
fancy-regex = { workspace = true }
