[package]
name = "tidebreak-mcp"
description = "Tidebreak MCP: the Model Context Protocol server face and client for mounting external tool servers."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
async-trait = { workspace = true }
base64 = { workspace = true }
futures = { workspace = true }
image = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
# Only the tool contracts (Tool / ToolRegistry / ToolCtx / …), not the SQLite /
# keychain / agent-loop machinery — hence default-features = false.
tidebreak-core = { path = "../tidebreak-core", default-features = false }

[dev-dependencies]
axum = { workspace = true }
chrono = { workspace = true }
