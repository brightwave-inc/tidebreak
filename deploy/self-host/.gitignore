# What setup.sh writes: the operator's credential file and the key that
# encrypts stored secrets. Never commit either. `.env` is already covered by
# the repository-root .gitignore.
tokens
secret.key
